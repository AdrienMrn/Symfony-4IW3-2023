{% extends 'base.html.twig' %}

{% block title %}Review{% endblock %}

{% block body %}
    <h1>Review</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ review.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ review.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_app_review_index') }}">back to list</a>

    <a href="{{ path('back_app_review_edit', {'id': review.id}) }}">edit</a>

    {{ include('back/review/_delete_form.html.twig') }}
{% endblock %}
