{% extends 'base.html.twig' %}

{% block title %}Edition du film {{ movie.name }}{% endblock %}

{% block body %}
    <h1>Edition du film {{ movie.name }}</h1>

    {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit">Modifier</button>
    {{ form_end(form) }}
{% endblock %}
